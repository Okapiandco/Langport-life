# Copy to .env.local and fill in. Real values live in Vercel env vars — never commit them.

# Sanity
NEXT_PUBLIC_SANITY_PROJECT_ID=8ecf405k
NEXT_PUBLIC_SANITY_DATASET=production
SANITY_API_TOKEN=
SANITY_REVALIDATE_SECRET=

# Email (Resend)
RESEND_API_KEY=
RESEND_FROM_EMAIL=
MODERATION_RECIPIENT=
CONTACT_RECIPIENT=

# Vercel cron authentication
CRON_SECRET=

# Maps (optional)
NEXT_PUBLIC_MAPBOX_TOKEN=

# Google Analytics (GA4) — only loads after a visitor accepts cookies
NEXT_PUBLIC_GA_MEASUREMENT_ID=
